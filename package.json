{
  "name": "smart-contract-2.0-linera",
  "version": "1.0.0",
  "description": "AI-Enhanced Smart Contracts on Linera Blockchain",
  "type": "module",
  "scripts": {
    "build:linera": "cd linera-integration && cargo build --release --target wasm32-unknown-unknown",
    "deploy:linera": "cd linera-integration && ./scripts/deploy.sh",
    "test:oracle": "cd linera-integration/oracle-client && node test.js",
    "vercel-build": "echo 'Build complete'",
    "vercel-deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-graphql": "^0.12.0",
    "graphql": "^16.12.0",
    "graphql-request": "^6.1.0"
  }
}
